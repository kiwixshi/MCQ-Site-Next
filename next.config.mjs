/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = {
    output: 'export',
    assetPrefix: './',
    images: {
      unoptimized: true,
    },
  };

export default nextConfig;
